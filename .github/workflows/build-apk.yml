name: Build BacaKomik APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Install npm dependencies
        run: npm ci

      - name: Verify Capacitor install
        run: |
          ls -la node_modules/.bin/cap || true
          ls -la node_modules/@capacitor/cli/bin/ || true

      - name: Capacitor sync Android
        env:
          CI: "true"
        run: |
          node --version
          node node_modules/@capacitor/cli/bin/capacitor --version
          node node_modules/@capacitor/cli/bin/capacitor sync android --inline --verbose 2>&1
          echo "EXIT_CODE=$?"

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Rename APK
        run: |
          mkdir -p dist
          cp android/app/build/outputs/apk/debug/app-debug.apk dist/BacaKomik.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BacaKomik-apk
          path: dist/BacaKomik.apk
          retention-days: 30

      - name: Create release on tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/BacaKomik.apk
